<%- include('header',{signin: "<script src=\"/script/index.js\"></script>"}) -%>
<%- include('menu',{ page: "main"}) -%>


<div class="container">
	<div class="row">
		<%
 			if(SWarr.length == 0){
				%>
				<div class="col-lg-12 col-md-12" style="text-align: center">
					<div class="alert alert-warning" role="alert">
						<b>Nincs elérhető kapcsoló</b>
					</div>
				</div>
				<%
			}else{
 		%>
			<% for( key in SWarr){ var item = SWarr[key]; %>
				<%- include('switch/basic', { item }); %>
		 <% }
		}%>
	</div>
</div>
<%- include('footer') -%>